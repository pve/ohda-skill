---
name: ohda
description: PLACEHOLDER — details pending. Will formalize the OHDA (Observation/Hypothesis/Decision/Action) troubleshooting-worklog convention used in non-trivial diagnosis sessions.
version: 0.1.0
---

# OHDA Worklog (draft)

Placeholder. This skill is being scaffolded ahead of its real content — see
the Peters Infra CLAUDE.md contract's "Diagnose-sessies" section for the
convention this formalizes:

- Non-trivial diagnoses keep an OHDA worklog in `~/tmp/<topic>/`, lines
  prefixed `O:`/`H:`/`D:`/`A:`, TL;DR at top, dead-ends stay.
- It's a working artifact outside the repo — only the conclusion promotes to
  `journal.md`/`state/`.

TODO: fill in trigger conditions, exact procedure, and any templates once
Peter provides the details.
